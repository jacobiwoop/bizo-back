import { PlaceholderScreen } from "@/src/components/ui/placeholder-screen";

export default function CreateNewPasswordRoute() {
  return (
    <PlaceholderScreen
      title="Create New Password"
      subtitle="L'ecran existe deja dans la nouvelle stack. Il sera ensuite lie aux endpoints backend de reset password."
    />
  );
}
