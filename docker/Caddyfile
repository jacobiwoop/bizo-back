{
    auto_https off
    admin off
}

:{$PORT:10000} {
    encode zstd gzip

    @reverb {
        path /app* /apps*
    }

    reverse_proxy @reverb 127.0.0.1:{$REVERB_SERVER_PORT:8080}

    root * /app/public
    php_server
}
